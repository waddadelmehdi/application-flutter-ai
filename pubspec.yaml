name: application
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  speech_to_text: ^7.0.0
  google_generative_ai: ^0.4.6
  firebase_core: any
  firebase_auth: any
  firebase_storage: any
  cloud_firestore: any
  image_picker: ^1.1.2
  tflite_flutter: ^0.9.5
  tflite_flutter_helper: ^0.2.1
  camera: ^0.11.0
  image: ^3.0.1
  tflite: ^1.1.2
  path_provider: ^2.0.13
  permission_handler: ^11.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/pngegg.png
    - assets/models/cnn_fruits_model.tflite  # Ensure this is correctly referenced
    - assets/models/labels.txt
  uses-material-design: true


